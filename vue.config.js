// vue.config.js
module.exports = {
    css: {
      loaderOptions: {
        sass: {
          // Use the sass-loader options if needed
          implementation: require('sass'),
        },
      },
    },
};